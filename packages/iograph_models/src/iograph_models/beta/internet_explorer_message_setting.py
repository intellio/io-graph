from __future__ import annotations
from enum import StrEnum


class InternetExplorerMessageSetting(StrEnum):
	notConfigured = "notConfigured"
	disabled = "disabled"
	enabled = "enabled"
	keepGoing = "keepGoing"

