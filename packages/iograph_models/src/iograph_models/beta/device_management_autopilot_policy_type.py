from __future__ import annotations
from enum import StrEnum


class DeviceManagementAutopilotPolicyType(StrEnum):
	unknown = "unknown"
	application = "application"
	appModel = "appModel"
	configurationPolicy = "configurationPolicy"

